# 정식 오픈 전 체크리스트

**이 문서가 배포 게이트다.** 여기 전부 통과하지 않으면 실사용 오픈하지 않는다.

세부 근거는 [TECH_SPEC.md §10](TECH_SPEC.md) 에 있다. 이 문서는 **실제로
되돌려야 하는 것**을 앞에 모아둔 것이다.

---

## A. 개발 편의로 껐다가 반드시 되돌릴 것

가장 잊기 쉬운 항목이다. **개발 중에는 끄는 게 맞지만, 이 상태로 오픈하면
사고가 난다.**

### A-1. 이메일 인증 (Confirm email) ⚠️ 현재 꺼짐

| | |
|---|---|
| 껐을 때 | 가입 폼 제출만으로 즉시 이용 가능. 개발이 빠르다 |
| 오픈 시 위험 | **남의 이메일 주소로 무제한 가입이 가능하다.** 존재하지 않는 주소로도 계정이 생기고, 스팸 가입을 막을 수단이 없다 |
| 되돌리는 곳 | `https://supabase.com/dashboard/project/_/auth/providers?provider=Email` → **Confirm email** 켜기 |
| 확인 | 새 이메일로 가입 → 인증 메일이 와야 하고, 링크 전에는 로그인이 안 되어야 한다 |

> **앱이 알려준다.** 이 설정이 꺼져 있는 동안 사이트 상단에 노란
> `개발 모드` 배너가 뜬다. 다시 켜면 자동으로 사라진다
> (`components/layout/DevAuthBanner.tsx`). **배너를 코드에서 지우지 말 것** —
> 지우면 정작 필요한 순간에 경고가 없어진다.

- [ ] Confirm email 켜기
- [ ] 배너가 사라진 것 확인
- [ ] 인증 없이 만든 개발용 계정 정리 (`Authentication → Users`)

### A-2. 검색엔진 색인 ⚠️ 현재 차단됨

지금은 의도적으로 막아둔 상태다. 검토 전 법적 고지문과 미확정 사업자 정보가
색인되면 지워도 캐시에 남는다.

- [ ] 법적 고지문 변호사 검토 완료 (PRD 부록 A) — **이게 먼저다**
- [ ] 사업자 정보 확정 (`config/company.ts`)
- [ ] Vercel 환경변수에 `ALLOW_INDEXING=true` 추가 후 재배포
- [ ] `/robots.txt` 가 `Disallow: /` 가 아닌 것 확인
- [ ] 홈 소스에 `noindex` 가 없는 것 확인

### A-3. 개발용 계정·데이터 정리

- [ ] 테스트 회원 삭제 — `supabase/drop_test_member.sql`
- [ ] 검증용으로 쓴 글·댓글 삭제
- [ ] 시드 코드 제거 (`SUPABASE_SETUP.md` §8)
- [ ] `supabase/create_admin.sql` 로 만든 계정의 비밀번호를 강한 값으로 변경

---

## B. 메일 발송

- [ ] **커스텀 SMTP 연결** (Resend / SendGrid / SES / Postmark)
      — 내장 메일러는 시간당 발송 수가 몇 통 수준이라 실사용 불가
- [ ] 발신 도메인 SPF · DKIM · DMARC
- [ ] 이메일 템플릿을 `{{ .TokenHash }}` 형태로 유지 (`SUPABASE_SETUP.md` §4.6)
- [ ] 가입·비밀번호 재설정 메일 실제 수신 테스트

> 이메일 인증을 껐어도 **비밀번호 재설정은 메일이 필요하다.** SMTP 없이는
> 비밀번호를 잊은 회원을 구제할 수 없다.

## C. 인증 보안

- [ ] 최소 비밀번호 길이 10자 + 문자 조합 요구
- [ ] **유출 비밀번호 차단(HaveIBeenPwned)** — **Pro 플랜 필요**
- [ ] `Attack Protection → CAPTCHA` 활성화 — **클라이언트 코드 수정 동반**
- [ ] `Rate Limits` 확인
- [ ] 글·댓글 도배 방지 쿨다운

## D. 접근 제어

- [ ] 신규 테이블 전부 RLS 활성화
- [ ] `Advisors → Security Advisor` 경고 0건
- [ ] RLS 테스트 통과 (`npm run test:rls`)
- [ ] `SECURITY DEFINER` 함수의 `grant` 범위 재확인
- [ ] `service_role` / `sb_secret_` 키가 코드·환경변수·로그에 없는지 확인

## E. 주소·인프라

- [ ] 자체 도메인 + HTTPS
- [ ] Supabase `Site URL` 을 실제 도메인으로
- [ ] `Redirect URLs` 에 `/auth/confirm` `/auth/callback` 등록 (와일드카드 금지)
- [ ] Vercel `SITE_URL` 갱신 후 재배포
- [ ] **Vercel Hobby → Pro** (유료화·광고 시점. Hobby 는 상업적 사용 제한)
- [ ] Supabase Free → Pro (미사용 시 일시 정지, 용량·발송 한도)

## F. 운영

- [ ] DB 자동 백업 + **복구 절차 1회 리허설**
- [ ] 오류 추적(Sentry) 연결
- [ ] 개인정보 보관·파기 정책 확정 → 처리방침 반영
- [ ] 감사 로그가 실제로 남는지 확인 (`/admin` 하단)
- [ ] 저장소 Private 전환 검토 (실제 사업자 정보 반영 시점)

## G. 규제·표기 (PRD §10)

- [ ] **법적 고지문 변호사 검토** (부록 A 3종)
- [ ] 커뮤니티 트랙 불변조건 I-1~I-4 유지 확인
- [ ] 금지 기능(수익률 인증·랭킹, 종목 추천) 부재 확인
- [ ] 더미 사업자등록번호·주소·전화번호 없음
- [ ] 서비스명 상표 확인 (KIPO)
- [ ] 관리자 운영 수칙 문서화 (D-5)
- [ ] 전 페이지 푸터 고지 노출

---

## 오픈 직전 최종 확인

| # | 확인 | 기대 |
|---|---|---|
| 1 | 상단에 `개발 모드` 배너가 없다 | Confirm email 켜짐 |
| 2 | 새 이메일로 가입 | 인증 메일 수신, 링크 전 로그인 불가 |
| 3 | 비밀번호 재설정 | 메일 수신, 링크로 재설정 완료 |
| 4 | `/robots.txt` | `Disallow: /` 가 아니다 |
| 5 | 푸터 사업자 정보 | `설립 절차 진행 중` 이 아니다 |
| 6 | `npm run test` | 전부 통과 |
| 7 | `/advisory` | 인허가 전이면 404 유지 |
