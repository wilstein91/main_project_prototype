# 영웅호걸닷컴 (가칭)

투자정보 공유 커뮤니티 플랫폼. 개인 투자자들이 정보와 의견을 나누는 게시판 서비스다.
나중에 같은 도메인 위에 투자자문사 영역을 얹지만, **커뮤니티는 그와 무관하게
전 기능을 개발·공개한다** (트랙 분리 — PRD §3).

배포: https://namjosunhero.vercel.app · 색인 차단 중 · 태그 `phase1-v1.1`

| 문서 | 용도 |
|---|---|
| [HANDOFF.md](HANDOFF.md) | **새 세션은 여기부터** ← 현재 상태·다음 할 일·함정 |
| [PHASES.md](PHASES.md) | 페이즈별 할 일 (Phase 0~5) |
| [WORK_UNITS.md](WORK_UNITS.md) | 티켓별 구현·검증 근거 |
| [DESIGN.md](DESIGN.md) | 디자인 방향 — 로고·색·타이포·톤 |
| [FINAL_CHECKLIST.md](FINAL_CHECKLIST.md) | **오픈 전 최종 관문** (기능·비정상 접근·보안) |
| [PRD.md](PRD.md) | 무엇을 만드는가 — 요구사항, 트랙 분리, 법적 고지문 |
| [TECH_SPEC.md](TECH_SPEC.md) | 어떻게 만드는가 — 아키텍처, 스키마·RLS, 티켓 정의 |
| [SUPABASE_SETUP.md](SUPABASE_SETUP.md) | DB 연결 절차 |
| [DEPLOY.md](DEPLOY.md) | 배포 절차 |

---

## 현재 상태 — Phase 1 v1.1

**Phase 1 기능은 전부 동작하고 프로덕션에 올라가 있다.** 실제 Supabase 에
연결되어 회원가입·글쓰기·댓글이 실동작하고, 디자인 기반 작업(P1-D)도
전부 끝났다. Phase 1 에 남은 것은 실기기 QA 와 전환 리허설 둘뿐이며
코드 작업이 아니다.

| | |
|---|---|
| 스택 | Next.js 16 (App Router) · TypeScript · Tailwind 4 · Supabase · Vercel |
| DB | Postgres + RLS 정책 14개, 마이그레이션 0001~0006 |
| 테스트 | 단위 121개 · RLS 통합 37개 — **전부 통과** (실DB 대상) |
| 검사 | 타입·린트·프로덕션 빌드 통과 |
| 접근성 | 대비 4.5:1 · 터치 44px · 키보드 초점 — 렌더된 화면에서 실측 통과 |

### 구현된 기능

| 영역 | 기능 | |
|---|---|---|
| **인증·계정** | 이메일 회원가입 (약관 동의 3종) | ✅ |
| | 로그인 · 로그아웃 · 세션 자동 갱신 | ✅ |
| | 닉네임 정책 — 2~12자, 중복 불가, 30일 1회 변경 | ✅ |
| | 프로필 설정 · 회원 탈퇴 | ✅ |
| | 비밀번호 재설정 | 🔶 코드 완료, **메일 발송 미구성** |
| **게시판** | 카테고리 5개 (공지·자유·종목토론·시황뉴스·질문답변) | ✅ |
| | 전체 통합 최신글 피드 · 카테고리별 목록 · 페이지네이션 | ✅ |
| | 글 작성 · 수정 · 삭제(소프트 삭제) | ✅ |
| | 마크다운 렌더 + sanitize (script·iframe·onerror 제거) | ✅ |
| | 조회수 — 쿠키로 같은 방문자 재조회 제외 | ✅ |
| | 공지 상단 고정 — 관리자만 (폼 조작으로도 불가) | ✅ |
| **댓글** | 댓글 · 대댓글 1단계 · 수정 · 삭제 | ✅ |
| | 댓글 수 자동 동기화 (DB 트리거) | ✅ |
| **관리자** | `/admin` — proxy + 서버 컴포넌트 이중 권한 확인 | ✅ |
| | 타인 글 삭제 시 감사 로그 기록 (관리자만 열람) | ✅ |
| **정책·법적** | 서비스 소개 · 이용약관 · 개인정보처리방침 · 투자 유의사항 | ✅ |
| | 자문사 트랙 게이트 — `/company` 준비 중, `/advisory` 404 | ✅ |
| | 사업자 정보·고지문을 `config/` 3개 파일로 분리 (설립 시 파일만 교체) | ✅ |
| | sitemap · robots 게이트 연동, 색인 차단 | ✅ |
| **보안** | RLS 로 전 테이블 보호, 권한 판정은 DB 에서 | ✅ |
| | 컬럼 단위 GRANT — 회원이 자기 `role` 을 변경 불가 | ✅ |
| | SECURITY DEFINER 함수의 EXECUTE 를 역할별로 회수 | ✅ |
| | 오픈 리다이렉트 방어 (공격 19종 테스트 고정) | ✅ |
| | 세션 쿠키 `httpOnly`·`secure`·`sameSite` | ✅ |
| | 보안 헤더 (CSP 일부 · nosniff · frame 차단 · referrer) | ✅ |
| **디자인** | 반응형 단일 코드베이스 — PC 사이드바 / 모바일 하단 탭 | ✅ |
| | Pretendard 자체 호스팅 (동적 서브셋, 외부 요청 0) | ✅ |
| | 청룡언월도 로고 — 작은 마크 · 삽화 · 가로형 3종 | ✅ |
| | 목록 행 · 색 토큰 · 타이포 스케일 정리 | ✅ |
| | 여백 리듬 4px 배수 통일 — 모든 화면에서 글 시작 위치가 같다 | ✅ |
| | 문안 톤 통일 — 빈 상태 `사실 + 다음 행동`, 오류 `~해 주세요` | ✅ |
| **접근성** | 대비 4.5:1 — 렌더된 화면에서 실측 | ✅ |
| | 터치 타겟 44px (`pointer: coarse` 기기에서) | ✅ |
| | 키보드 초점 표시 · 오류를 `role="alert"` 로 안내 | ✅ |

**미구현 (Phase 1 잔여)** — 실기기 QA(iOS·Android), 전환 리허설,
커스텀 SMTP. 자세한 칸은 [PHASES.md](PHASES.md).

### 버전 기록

| 버전 | 날짜 | 내용 |
|---|---|---|
| **phase1-v1.1** | 2026-09-09 | **P1-D 완료** — 여백 리듬 4px 배수 통일(화면을 옮겨도 본문이 좌우로 튀지 않는다) · 문안 톤 통일 · 접근성 실측. 화면으로는 안 보이던 3건 수정: 보조 텍스트가 **브랜드색 면 위에서만 4.31:1** 미달, 입력칸의 `outline-none` 으로 **키보드 초점이 보이지 않던 것**, 오류 메시지가 **회원이 할 수 없는 일(SMTP 설정)을 안내**하던 것 |
| **phase1-v1.0** | 2026-09-09 | 청룡언월도 로고 3종 · 목록 행 재구성 · 시드 콘텐츠 26건 · **보안 취약점 4건 수정** (오픈 리다이렉트 우회, 세션 쿠키 노출, 보안 헤더 누락, SECURITY DEFINER 함수의 anon 권한 회수 누락) |
| — | 2026-09-08 | Phase 1 기능 전부 프로덕션 검증. 테스트 도입 중 **권한 상승 취약점** 발견·수정 |
| — | 2026-09-08 | Vercel 배포, Supabase 연결, 스키마·RLS 적용 |
| — | 2026-09-08 | Phase 1 화면 형상 + 이중 모드(시드/실DB) 구조 |

> 취약점 4건은 전부 **테스트와 직접 찔러보기로** 찾았다. 화면으로는
> 아무 증상이 없었다. 그래서 [FINAL_CHECKLIST.md](FINAL_CHECKLIST.md) §C 가
> 수동 확인이 아니라 실행 가능한 명령·SQL 로 되어 있다.

---

## 다음 계획

각 페이즈의 전체 목록과 판단 근거는 [PHASES.md](PHASES.md) 에 있다.

| 페이즈 | 목표 | 대표 항목 |
|---|---|---|
| **Phase 2** 커뮤니티 활성화 | 사람이 머무는 이유를 만든다 | 추천/비추천, 인기글(조회·반응 기준), 검색, 이미지 업로드, 프로필 페이지, 인앱 알림, 신고 처리, **커스텀 SMTP**, 도배 방지 |
| **Phase 3** 정착 | 주제별로 굴러가게 한다 | 종목 태그·태그 피드, 활동 기반 등급(수익률 아님), FAQ·문의. 자문사 트랙: 리서치 게시판, `/advisory` — **신고·등록 후 공개** |
| **Phase 4** 확장·수익화 | 돈과 채널을 붙인다 | 커뮤니티 편의 구독(광고 제거 등), 카카오 로그인, 알림톡, 실시간 채팅, 운영 통계. 자문사 트랙: 유료 투자정보 멤버십 — **등록 + 결제 심사 후** |
| **Phase 5** 안정화 | 오래 굴러가게 한다 | 모니터링·오류 추적, 백업·복구 리허설, SEO 고도화, 접근성 감사, 다크 테마 검토 |

**수익률·추천 기준의 랭킹은 어느 페이즈에도 없다.** 그걸 넣는 순간
커뮤니티 트랙이 규제 대상이 된다 (아래 '지켜야 하는 것').

---

## 실행

```bash
npm install
npm run dev
```

기본 포트는 3000, 이 저장소의 `.claude/launch.json` 은 **3200** 을 쓴다.
환경변수는 `.env.local.example` 을 복사해 채운다.

```bash
cp .env.local.example .env.local
```

## 명령

| 명령 | 설명 |
|---|---|
| `npm run dev` | 개발 서버 |
| `npm run test` | 단위 테스트 (DB 불필요) |
| `npm run test:rls` | RLS·비정상 접근 통합 테스트 (실 DB + 테스트 계정 필요) |
| `npm run build` | 프로덕션 빌드 |
| `npx next typegen` | 라우트 타입 재생성 (`PageProps` 오류 시) |
| `npx tsc --noEmit` | 타입 검사 |
| `npm run lint` | ESLint |
| `python scripts/build_logo.py [이름]` | 로고 SVG 재생성 |

## 구조에서 먼저 볼 것

| 경로 | 왜 중요한가 |
|---|---|
| `src/config/company.ts` | 사업자 정보의 **단일 출처**. 설립 후 이 파일만 고친다. 미확정 값은 `null` |
| `src/config/features.ts` | 트랙 구분과 공개 게이트. 커뮤니티 기능은 `'live'` 리터럴만 받아 규제로 막힐 수 없다 |
| `src/config/legal.ts` | 법적 고지문. 컴포넌트에 하드코딩하지 않는다 |
| `src/lib/data/queries.ts` | 데이터 접근 진입점. 키 유무로 시드/실DB 를 고른다 |
| `src/lib/actions/` | Server Actions. 데이터 변경은 전부 여기로만 |
| `src/lib/safe-path.ts` | 리다이렉트 검증. `?redirect=` 를 쓰는 모든 곳이 이걸 지난다 |
| `src/lib/supabase/cookie-options.ts` | 세션 쿠키 `httpOnly` 강제. 지우면 토큰이 스크립트에 노출된다 |
| `src/proxy.ts` | 세션 갱신 + 보호 경로. Next 16 규약 (middleware 아님) |
| `supabase/migrations/` | 스키마·트리거·RLS. 번호 순서대로 적용한다 |

## 지켜야 하는 것

**커뮤니티 트랙은 규제 게이트가 없다.** 이를 유지하려면 네 가지를 지켜야 한다 (PRD §3.2).

1. 운영자는 투자 의견을 발행하지 않는다 (운영 공지만)
2. 투자정보의 대가를 받지 않는다 (유료화는 편의 기능 한정)
3. 서비스가 종목 추천·매매 신호를 만들지 않는다
4. 수익률을 표방하지 않는다 (수익률 인증·랭킹 없음)

신규 기능을 커뮤니티 트랙에 추가할 때마다 위 네 가지를 확인한다. 어긋나면 자문사 트랙으로 옮기고 게이트를 건다.

**`service_role`(secret) 키는 사용하지 않는다.** 모든 데이터 접근은 사용자 JWT 로 RLS 판정을 받는다. 이 키 하나로 접근 제어가 통째로 우회된다.

**권한 검사는 DB 에 둔다.** 화면의 버튼 숨김과 페이지의 `redirect`/`notFound` 는 편의일 뿐이다. 최종 판정은 RLS 정책이 한다 — 우회해도 DB 가 거부해야 정상이다.

**막히는 것만 테스트하지 않는다.** 통해야 하는 동작도 같은 강도로 검사한다.
`남의 글을 삭제할 수 없다` 가 통과하는데 실은 **본인 글까지 모든 삭제가
막혀 있던** 버그가 있었다. 금지만 검사하면 이런 실패를 못 잡는다.

**사용자 본문은 반드시 sanitize 를 통과시킨다.** `PostBody` 가 `rehype-sanitize` 로 처리한다. `dangerouslySetInnerHTML` 은 쓰지 않는다.

**`"use server"` 모듈은 모든 export 가 async 함수여야 한다.** 상수나 동기 헬퍼를 같이 내보내면 모듈 전체의 export 가 사라진다 (`src/lib/profile-rules.ts` 가 그래서 분리되어 있다).

**Next.js 16 이다.** `middleware.ts` 가 아니라 `proxy.ts` 이고, `params`·`cookies()` 는 완전 비동기다. API 를 기억으로 쓰지 말고 `node_modules/next/dist/docs/` 를 확인한다.

## 배포 전

[FINAL_CHECKLIST.md](FINAL_CHECKLIST.md) 를 전부 통과해야 한다.

**지금 개발 편의로 꺼둔 것이 두 개 있다** — 이메일 인증, 검색엔진 색인.
둘 다 되돌리는 방법이 체크리스트 D 에 있다. 이메일 인증이 꺼져 있는 동안은
사이트 상단에 `개발 모드` 배너가 뜬다. **배너를 코드에서 지우지 말 것** —
지우면 정작 필요한 순간에 경고가 없어진다.

DB 에 시드 글 26건과 시드 계정 7개가 있다 (체크리스트 D-3 에 목록).
법적 고지문(PRD 부록 A)은 변호사 검토 전 초안이다.
