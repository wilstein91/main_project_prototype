# 디자인 방향

[PHASES.md](PHASES.md) 의 **P1-D 베이스 디자인 개선** 이 따르는 문서다.
할 일 목록은 그쪽에, 판단 기준은 여기에 있다.

---

## 1. 무엇을 만드는가

개인 투자자가 정보와 의견을 나누는 게시판. 이용자는 30~50대, 모바일
비중이 높다. 화면이 해야 하는 일은 세 가지다 — **빨리 읽기, 부담 없이
쓰기, 여기 있어도 되겠다는 느낌.**

이름이 방향을 정해 준다. `영웅호걸` 은 삼국지의 어휘다. 그래서 시각적
출처를 삼국지에서 가져오고, 그 중 **청룡언월도**를 로고로 줄여 쓴다.

**왜 언월도인가.** 금융 사이트의 기본값은 차트·화살표·방패다. 언월도의
초승달 날은 그 어느 것과도 닮지 않아서 작게 줄여도 알아본다. 이름과
직접 이어지고, 설명이 필요 없다.

## 2. 톤 — 편안한 격

깔끔하게 유지하되 무겁지 않다. 삼국지의 비장함이 아니라 **판이 벌어진
자리의 편안함**을 쓴다. 장난스럽지 않고, 격식으로 겁주지도 않는다.

| 자리 | 태도 |
|---|---|
| 글 목록 · 상세 · 작성 폼 | 무표정. 정보만. 여기서 사람들은 돈 얘기를 읽는다 |
| 정책 페이지 · 사업자 정보 | 완전한 격식체 |
| 서비스 소개 · 빈 상태 · 404 | **여기만** 브랜드의 목소리가 나온다 |
| 오류 화면 | 농담 금지. 무엇을 하면 되는지만 |
| 헤더 · 사이드바 · 하단 탭 | 마크는 작게 한 번. 삽화 금지 |

읽는 길은 조용하고, **사용자가 멈춘 자리에만** 색과 그림이 나온다.
이 배치가 "깔끔하되 편안한" 을 만드는 방법이다.

### 2.1 빈 상태 — 사실 한 줄 + 다음 행동 한 줄

| | |
|---|---|
| 사실 | `아직 ~가 없습니다` 로 시작한다. '비어 있음' 은 고장이 아니므로 사과하지 않는다 |
| 다음 행동 | `~해보세요` 한 줄. 사용자가 할 수 있는 일이 없으면(운영자 전용 게시판) 왜 없는지를 대신 쓴다 |
| 금지 | 느낌표, 사과, "죄송합니다", 감탄사 |

```
아직 글이 없습니다
첫 글을 남겨보세요.              ← 회원이 쓸 수 있는 게시판

아직 글이 없습니다
이 게시판에는 운영자만 글을 쓸 수 있습니다.   ← 쓸 수 없는 게시판
```

같은 상황에는 같은 문장을 쓴다. 홈과 게시판 목록이 서로 다른 말("이곳에
표시됩니다" / "남겨보세요")을 하고 있었는데, 같은 빈 목록이므로 같은
문장이어야 한다.

### 2.2 오류 — 무슨 일이 있었는지 + 무엇을 하면 되는지

| | |
|---|---|
| 사실 | `~하지 못했습니다` / `~이 올바르지 않습니다`. 원인을 모르면 추측하지 않는다 |
| 다음 행동 | `~해 주세요`. 어투는 화면 전체에서 이것 하나로 통일한다 (`~하세요` 를 섞지 않는다) |
| 금지 | 농담, 사과, 느낌표, **사용자가 할 수 없는 일** |

마지막 항목이 실제로 문제가 됐다. 메일 발송 한도 오류에 "커스텀 SMTP 를
연결해 주세요" 라고 적혀 있었다. 회원이 할 수 있는 일이 아니다. 운영자가
봐야 하는 내용은 **서버 로그로** 보내고, 화면에는 사용자가 실제로 할 수
있는 것만 남긴다.

`~하세요` 는 입력칸 안내문(placeholder)에만 쓴다. 오류 메시지는 전부
`~해 주세요` 다. 문구는 [`lib/actions/result.ts`](src/lib/actions/result.ts)
한 곳에 모여 있다 — 같은 문장을 두 곳에 쓰지 않는다.

## 3. 로고

> **v1.3 — 로고는 그림 한 장이다.** 청룡언월도 위에 명조 워드마크를 얹어
> `lockup.svg` 한 파일로 박제했다 (§3.5). 직접 그린 SVG 마크는 버렸다 —
> 생성형 AI 로 뽑은 그림이 비교가 안 되게 낫다.
>
> 아래 §3.1~§3.3 은 **그때 배운 것을 남긴 기록**이다. 20px 에서 형태보다
> 색이 오래 살아남는다는 결론은 지금도 유효하고, §3.4 의 워드마크 제작
> 절차는 **여전히 쓰인다** — 합성 로고의 글자가 그것이다.

### 3.1 마크

청룡언월도를 **글자처럼 읽히는 정도까지** 줄인다.

```
   구성                     24px 로 줄일 때
   ┌─────────────┐          ┌─────────┐
   │   ╭──╮      │          │   ╭─╮   │   초승달 날 + 자루만 남긴다
   │  ╱    ╲     │          │  ╱   ╲  │   등날 갈고리는 버린다
   │ │  날  │    │          │ │     │ │
   │  ╲    ╱     │          │  ╲   ╱  │
   │   │자루│    │          │   │ │   │
   │   │    │    │          └─────────┘
   │   ╰ 갈고리  │
   └─────────────┘
```

| 규칙 | |
|---|---|
| 각도 | 약 20° 기울인다. 기울기는 **좌표에 직접 넣는다** — `rotate()` 는 도형을 viewBox 밖으로 밀어 날 끝을 자른다 |
| 날 모양 | 등(위)은 볼록, 인(아래)은 **오목**. 둘 다 볼록하면 렌즈가 되어 잎사귀로 읽힌다 |
| 선 | 만화 컬러판의 잉크선. 굵기 고정 |
| 채색 | **4색** — 먹 · 청록 · 금 · 주홍. 금은 투겁과 물미, 주홍은 술에만 |
| 그라디언트 | 없음. 컬러판은 평면 채움이다 |
| 최소 크기 | 20px. 그 아래로는 갈고리를 버린다 (색은 끝까지 유지) |
| 금지 | 회전 애니메이션, 반복 배경 패턴, 워터마크 |

#### 네 번 고치고 배운 것

v1 은 날이 얇아 사선 획, v2 는 자루가 묻혀 초승달, v3 은 **잎사귀**로
읽혔다. 세 번 다 형태만 만졌다.

20px 에서 형태로 구별되는 특징은 서너 개가 한계다. 반면 **색은 형태보다
작아져도 살아남는다.** v4 에서 금 투겁과 붉은 술을 넣자 잎사귀·붓·도끼와
한눈에 갈렸다. 작은 마크에서 색은 장식이 아니라 식별 정보다.

#### 두 개로 나눈다

| 컴포넌트 | 크기 | 왜 |
|---|---|---|
| `BladeMark` | 20~34px | 특징을 서너 개로 줄인 것. 헤더·인증 화면 |
| `BladeCrest` | 76~150px | 물미·자루 결·하이라이트·술 두 갈래까지 |

작은 마크를 100px 로 확대하면 빈 도형이 된다. 반대로 삽화를 20px 로
줄이면 뭉친다. **실루엣(기울기·날 곡선)은 공유하고 디테일만 다르게**
가져간다 — 그래야 로고가 두 개로 보이지 않는다.

### 3.2 워드마크

**본문 서체와 로고 서체를 나눈다.** 본문은 Pretendard, 로고는 명조다.
로고는 한 번 그려 파일로 두는 것이므로 서체가 하나 늘어도 내려받는
용량이 늘지 않는다 (§3.4).

```
[마크] 영웅호걸닷컴          헤더 — 마크 25px, 글자 높이 21px
                             가칭 표기는 회색 11px 로 뒤에 (sm 이상)
```

처음에는 Pretendard Black 에 자간을 조여 썼다. 깔끔했지만 '영웅호걸'
이라는 이름과 어울리지 않았다. 지금은 §3.4 의 명조 윤곽선을 쓴다.

### 3.3 삽화가 나오는 자리

만화 컬러판 느낌을 **제대로 쓰는 곳은 세 군데뿐**이다.

| 자리 | 크기 | 내용 |
|---|---|---|
| 홈 상단 띠 | 92 / 132px | **비회원에게만.** 로그인하면 사라진다 |
| `/about` 히어로 | 116px | 이름의 근거를 설명하는 문단 옆에 |
| 404 | 88 / 104px | 언월도만. 농담 없음 |
| 빈 목록 | 76px, 40% 투명 | 문장보다 앞서지 않게 |

홈 상단 띠는 **처음 온 사람에게만** 보인다. 커뮤니티의 기본값은 목록이
바로 보이는 것이고(PRD §5), 매일 오는 회원에게 같은 소개를 반복하면
스크롤만 늘어난다. 브랜드는 사용자가 아직 할 일이 없을 때만 말을 건다.

**인물은 그리지 않는다.** 특정 만화의 캐릭터 디자인과 닮을 위험이 있고,
무엇보다 인물이 들어오면 화면이 무거워진다. 무기와 색만 가져온다.

### 3.4 가로형 로고

청룡언월도가 가로로 지나가고 그 위에 명조 워드마크가 얹힌다.
쓰는 곳은 **홈 상단 띠(비회원)와 /about** 뿐이다. 헤더처럼 작은 자리에는
§3.2 워드마크를 쓴다 — 가로형을 36px 로 줄이면 용머리와 손잡이 금테가
뭉친다.

#### 날은 원호로 그린다

베지에로 초승달을 흉내내면 두 곡선이 같은 방향으로 볼록해져 **렌즈**가
되고, 렌즈는 잎사귀로 읽힌다. 네 번 다시 그리면서 매번 여기 걸렸다.

초승달은 정의상 **같은 방향으로 휜 두 원호 사이 영역**이다. SVG 의
`A`(arc) 명령을 쓰면 그 기하가 보장된다.

```
M 34 22  A168 168 0 0 0  274 128   ← 인(아래): 반지름 작게 = 깊게 휜다
         A700 700 0 0 1   34 22    ← 등(위):   반지름 크게 = 얕게 돌아온다
```

두 반지름의 차이가 날의 두께다. 얕으면 칼, 깊으면 국자로 읽힌다.
168/700 이 칼로 읽히는 경계였다.

#### 글자는 서체가 아니라 윤곽선이다

궁서체는 웹에 쓸 수 없다. 윈도우 '궁서' 는 라이선스가 걸려 있고 웹
임베드가 허용되지 않는다. 대신 **나눔명조 ExtraBold** 를 쓴다 — SIL OFL
이라 웹 임베드와 윤곽선 변환이 모두 허용된다.

폰트 파일은 싣지 않는다. 한글 명조 한 벌은 1~2MB 인데 로고 여섯 자
때문에 그걸 받게 할 수 없다. 여섯 자만 윤곽선으로 변환해 정적 SVG 로
뽑았다.

| 파일 | 크기 | 쓰는 곳 |
|---|---|---|
| `public/logo/lockup.svg` | 17KB (gzip 6.9KB) | 홈 띠, /about |
| `public/logo/wordmark.svg` | 13KB | 헤더 (글자만) |

정적 파일로 두는 이유는 캐시다. React 컴포넌트로 두면 그 17KB 가
**페이지마다** HTML 에 다시 실린다. 대신 파일 안에 색이 굳어 있어
페이지 토큰을 따라가지 못한다 — 라이트 테마로 확정했으므로 지금은
문제되지 않는다.

#### 이름이 바뀌면

서비스명은 가칭이다. 윤곽선은 코드에서 글자를 고칠 수 없으므로 다시
뽑아야 한다.

```
python -m pip install fonttools brotli
python scripts/build_logo.py 새이름
```

스크립트가 구글 폰트에서 필요한 글자가 든 서브셋만 내려받아 윤곽선을
뽑고 두 파일을 다시 만든다. 무기 그림은 `scripts/_arm.svgfrag` 에 있다.
워드마크가 viewBox(900)를 넘으면 스크립트가 멈추고 알려준다.

### 3.5 합성 로고 — 겹침을 파일에 박제한다

헤더에서 청룡언월도와 워드마크를 **CSS 로 겹치면** 화면 폭에 따라 어긋난다.
좁은 화면에서는 무기가 버튼과 자리를 다투다 결국 숨기게 되고, 그러면
모바일에서만 로고가 달라진다.

`scripts/build_lockup.py` 가 둘을 합쳐 `public/logo/lockup.svg` 한 장으로
만든다. 화면에서는 `<img>` 하나의 높이만 조절한다.

| | |
|---|---|
| 글자 | 윤곽선(path). 어느 크기로 키워도 선명하다 |
| 무기 | base64 로 파일 안에 박는다 — 외부 참조는 `<img>` 로 부를 때 차단된다 |
| 글자 폭 | 무기의 **58%**. 더 크면 무기가 다 가려지고, 더 작으면 글자가 묻힌다 |
| 무기 크기 | **파일에서 읽는다.** 값을 적어 두면 그림을 다듬을 때마다 어긋난다 |

글자는 **세 겹**이다 — 어두운 외곽선(19) → 금테(9) → 크림 채움. 무기 위에
얹히므로 글자만 있으면 묻힌다.

이름이 바뀌면 `build_logo.py` 와 `build_lockup.py` 를 차례로 돌린다.

---

## 4. 색

### 4.1 왜 바꿨나

브랜드색이 `#3182f6` 이었다 — **토스의 파랑을 그대로 쓰고 있었다.** PRD 의
"토스처럼 가독성 있게" 는 정보 배치와 여백을 두고 한 말인데 색까지
가져왔다. 서비스 이름을 정한 시점에 색도 우리 것으로 바꿨다.

### 4.2 왜 청록인가

두 가지 이유가 겹친다.

**청룡언월도의 `청`.** 청룡은 청록의 용이다. 로고와 색이 같은 출처를
갖는다.

**한국 시세 색이 빨강·파랑이다.** 상승이 빨강, 하락이 파랑이므로 그 둘은
시세 의미에 예약해야 한다. 브랜드색으로 파랑을 쓰면 "이 버튼이 하락을
뜻하나" 하는 혼선이 생긴다. **청록은 한국 시세 표기에서 비어 있는
자리**다. 의미 충돌이 없다.

### 4.3 팔레트 (v1.2 — 먹빛)

**명도를 뒤집되 색상은 그대로 뒀다.** 청록이 브랜드색인 근거는 §4.2 그대로
유효하고, 어두운 바탕에서 읽히도록 **밝은 쪽 청록(로고 날 색)** 으로 옮겼다.

```
--canvas       #14100D   가장 깊은 바탕 — 먹에 갈색 기미
--surface      #1F1912   패널·면
--surface-lit  #2A221A   hover · 입력칸 안쪽
--line         #3A3026   구분선 (읽기용)
--field        #7A6752   입력칸 테두리 — 바탕 대비 3.51:1
--ink          #F2ECE0   본문 — 16.1:1
--ink-sub      #B5A892   보조 — 8.1:1 (청록면 위 5.8:1)
--brand        #4BB8AA   링크 · 강조 — 7.9:1
--brand-strong #7FC9BE   hover — 9.9:1
--brand-soft   #17332F   활성 카테고리 · 배지 바탕
--gold         #C8922A   프레임 테두리 — 6.9:1
--gold-lit     #ECCB72   금색 글자 — 12.0:1
--gold-dim     #6B5020   은은한 프레임 (늘 번쩍이지 않게)
--danger       #E5705F   삭제 · 오류 — 6.1:1
```

**예약 — Phase 2 이후 시세 표기용. UI 크롬에 쓰지 않는다.** 어두운 바탕에서
읽히도록 밝은 쪽으로 옮겼다. 상승=빨강, 하락=파랑이라는 한국 관례는 그대로.

```
--up    #EF5A68   상승 — 5.7:1
--down  #4D8FE8   하락 — 5.8:1
```

#### 로고·삽화 전용 — 윤곽선이 밝은 쪽이 되었다

만화 컬러판에서 **흰 바탕에 먹선**을 긋던 것을, **검은 바탕에 흰선**을 긋는
쪽으로 뒤집었다. 날·투겁·술의 색은 그대로다 — 색은 명도를 뒤집어도 정체성이
남는다 (§3.1 에서 배운 것과 같은 이치다).

```
--ink-lit      #B9B2A4   윤곽선 위 결
--blade        #3FA79A   날
--blade-lit    #7FC9BE   날 끝
--tassel       #C0392F   술
--tassel-dark  #8E2018
```

> `BladeMark`·`BladeCrest` 는 `--color-ink` 를 **stroke 로만** 쓰므로 토큰만
> 바꾸면 따라온다. 반면 `wordmark.svg` · `lockup.svg` 는 **파일 안에 색이
> 굳어 있어** 따로 뒤집어야 했다 (§3.4). 테마를 다시 바꾼다면 그 두 파일과
> `scripts/build_logo.py` 의 `COLORS`, `scripts/_arm.svgfrag` 를 잊지 말 것.

**예약 — Phase 2 이후 시세 표기용. UI 크롬에 쓰지 않는다.**

```
--up          #E02B3C   상승 (한국 관례)
--down        #1B64C8   하락
```

채도를 낮게 잡았다. 형광 청록은 암호화폐 사이트로 읽히고, 이 서비스는
차분해야 한다. **강조는 색을 밝혀서가 아니라 주변을 비워서** 만든다.

배경을 크림색으로 하지 않는 이유: 따뜻한 크림 바탕은 지금 흔해서 어디서
본 듯한 화면이 된다. 흰 바탕에 청록 기미의 면 색을 얹는 쪽이 깔끔하고
편안하다.

## 5. 타이포

**두 벌을 쓴다 — 본문은 Pretendard, 인터페이스는 나눔명조.**

v1.1 까지는 Pretendard 하나만 썼다. 깔끔했지만 화면이 여느 커뮤니티와
구별되지 않았고, `영웅호걸` 이라는 이름과도 어긋났다. 인터페이스의
목소리를 본문과 갈라 놓기로 했다.

| | 서체 | 쓰는 곳 |
|---|---|---|
| 본문 | **Pretendard** | 글 본문·목록 제목·댓글·설명문 |
| 인터페이스 | **나눔명조** | 화면 제목·메뉴·버튼·배지 |

**본문에는 명조를 쓰지 않는다.** 긴 글을 명조로 읽히면 눈이 훨씬 빨리
지친다. 이 서비스에서 사람들이 가장 오래 보는 것은 남이 쓴 글이다.

궁서체는 쓸 수 없다 — 윈도우 '궁서'는 라이선스가 웹 임베드를 허용하지
않는다. **나눔명조는 SIL OFL** 이라 임베드·재배포가 모두 허용되고, 삼국지
느낌은 명조 계열로 충분히 난다.

둘 다 자체 호스팅이며 `unicode-range` 로 쪼개져 있다. 브라우저는 화면에
실제로 쓰인 문자 구간만 받는다 — 명조 전체는 5.8MB 지만 제목 몇 줄이면
수십 KB 다. 다시 받으려면 `python scripts/fetch_myeongjo.py`.

`public/fonts` 에 self-host 한다 (동적 서브셋 92조각). 구글 폰트 CDN 을
쓰지 않는 이유는 개인정보와 초기 로딩 두 가지다.

**숫자에 `tabular-nums` 를 적용한다.** 조회수·댓글 수·시세가 목록에서
자리를 맞춰 세로로 정렬된다. 비례 숫자는 행마다 자리가 흔들린다.

| 토큰 | 크기 / 행간 | 쓰는 곳 |
|---|---|---|
| `--text-display` | 26px / 1.35 | 서비스 소개, 404 |
| `--text-post` | 22px / 1.4 | 글 상세 제목 |
| `--text-title` | 20px / 1.4 | 화면 제목 |
| `--text-body` | 16px / **1.75** | 본문 |
| `--text-list` | 15px / 1.5 | 목록 제목 |
| `--text-meta` | 13px / 1.4 | 작성자 · 시각 |
| `--text-fine` | 12px / 1.7 | 법적 고지 |

본문 행간을 1.7 → **1.75** 로 올린다. 긴 한글 문단에서 이 차이가 크다.
본문은 15px 아래로 내리지 않는다.

## 6. 목록 행 다시 짜기

지금 목록의 메타 줄이 이렇다.

```
제목                                              3
자유게시판 · 달러사냥꾼 · 09.06 · 조회 341
```

가운뎃점으로 이어 붙인 메타 줄은 **아무 사이트에나 붙는 기본값**이고,
모바일에서 줄바꿈이 지저분하게 끊긴다. 구두점 대신 **자리와 간격**으로
구분한다.

```
모바일                              PC
┌────────────────────────┐          ┌──────────────────────────────────┐
│ [공지] 제목이 두 줄까지 │  3       │ [공지] 제목            달러사냥꾼 │
│ 넘칠 수 있다            │          │                  09.06  341   3  │
│ 달러사냥꾼      09.06   │          └──────────────────────────────────┘
└────────────────────────┘          숫자는 우측 정렬 + tabular-nums
```

- 카테고리 이름은 **전체 피드에서만** 보인다. 카테고리 안에서는 중복이다
- 조회수는 PC 에서만. 모바일에서는 정보 밀도가 아깝다
- 댓글 수는 숫자만. `댓글` 이라는 글자는 필요 없다
- 행 구분선을 줄이고 여백으로 나눈다 (지금은 표처럼 보인다)

## 7. 여백

목록 행·상세 헤더·폼이 각각 다른 리듬을 쓰고 있었다. **4px 배수 하나로
통일했다.** 새 화면을 만들 때는 이 표에 있는 값만 쓴다.

| 자리 | 값 | 클래스 |
|---|---|---|
| 화면 좌우 — 모바일 | 16px | `px-4` |
| 화면 좌우 — PC | 24px | `lg:px-6` |
| 목록 행 세로 | 16px | `py-4` |
| 카드 안쪽 — 머리·꼬리 | 20px | `py-5` |
| 카드 안쪽 — 본문 | 24px | `py-6` |
| 제목 아래 | 24px | `mb-6` |
| 소제목 아래 | 16px | `mb-4` / `gap-4` |
| 폼 필드 사이 | 20px | `gap-5` |
| 섹션 사이 | 32px | `gap-8` · `mt-8` · `pt-8` |

8px 이하의 미세 간격(글자와 배지 사이 같은 것)은 예외로 둔다. 리듬이
문제되는 것은 **블록 사이**지 글자 사이가 아니다.

#### 카드가 없는 화면도 같은 자리에서 시작한다

목록·상세는 PC 에서 테두리가 있는 카드 안에 들어가고 글자가 카드 안쪽
24px 에서 시작한다. 글쓰기·내 정보처럼 카드가 없는 화면은 `lg:px-0` 이라
글자가 24px 왼쪽에서 시작했고, 화면을 옮길 때마다 본문이 좌우로
움직였다. 카드가 없는 화면에도 `lg:px-6` 을 줘서 **글이 시작하는 x 좌표를
모든 화면에서 같게** 맞췄다. 카드를 새로 씌우지 않은 것은 문서·폼 화면에
테두리가 필요하지 않아서다.

## 8. 접근성

세 가지를 기준으로 잡는다. 실제로 재서 통과시켰다 — 눈으로 보고 "괜찮아
보인다" 로 넘기면 반드시 빠진다.

### 8.1 대비 — 어두운 바탕에서는 더 높게

본문 글자는 배경 대비 4.5:1 이상(큰 글자는 3:1)이 **최저선**이다. 어두운
바탕에서는 그 배 이상을 목표로 한다 (PRD V-1) — 게임 화면은 두세 줄을 읽지만
이 서비스는 긴 글을 읽는 곳이고, 대비가 모자라면 눈이 훨씬 빨리 지친다.

화면을 렌더한 뒤 모든 텍스트 요소의 실제 색과 실제 배경을 읽어 계산했다.

| 자리 | 값 |
|---|---|
| 본문 `--ink` / 바탕 | **16.1:1** |
| 보조 `--ink-sub` / 바탕 | 8.1:1 |
| 보조 `--ink-sub` / 면 `--surface` | 7.5:1 |
| 보조 `--ink-sub` / 브랜드면 `--brand-soft` | 5.8:1 |
| 브랜드 `--brand` / 바탕 | 7.9:1 |
| 금색 `--gold-lit` / 바탕 | 12.0:1 |
| 금 테두리 `--gold` / 바탕 | 6.9:1 |
| 오류 `--danger` / 바탕 | 6.1:1 |
| **먹색 글자 / 청록 버튼** | **7.9:1** |

마지막 줄이 v1.2 에서 바뀐 것이다. 주요 버튼은 **흰 글자를 쓰지 않는다** —
밝은 청록 위의 흰 글자는 **2.4:1** 까지 떨어져 읽히지 않는다. 글자를
바탕색(먹)으로 두면 7.9:1 이고, 에나멜 명판처럼 보여 톤과도 맞는다.

> 밝은 바탕을 어둡게 뒤집으면 **버튼 위 글자가 가장 먼저 깨진다.** 배경이
> 밝아졌는데 글자는 흰색 그대로이기 때문이다. 테마를 뒤집을 때 여기부터
> 본다.

<details>
<summary>v1.1 (흰 바탕) 실측값 — 참고용</summary>

| 자리 | 값 |
|---|---|
| 본문 `--ink` #15201F / 흰 바탕 | 16.7:1 |
| 보조 `--ink-sub` #5E6C69 / 흰 바탕 | 5.5:1 |
| 보조 / 브랜드면 #E4F0EE | 4.7:1 |
| 브랜드 #0F6E63 / 흰 바탕 | 6.1:1 |
| 흰 글자 / 브랜드 버튼 | 6.1:1 |

</details>

`--ink-sub` 를 `#64726F` → `#5E6C69` 로 한 칸 어둡게 했다. 흰 바탕과 면
위에서는 통과했지만 **브랜드색 면 위에서 4.31:1 로 미달**이었기 때문이다
(홈 상단 띠와 /about 히어로가 그 배색이다). 화면 하나를 고치는 대신
토큰을 고친 이유는, 토큰이 통과하지 못하면 앞으로 만드는 화면에서 같은
일이 반복되기 때문이다.

**입력칸 테두리는 따로 뒀다.** `--line`(#E0E6E4)은 흰 바탕에서 1.26:1 이라
읽기용 구분선으로는 맞지만 입력칸 테두리로는 경계가 보이지 않는다.
"여기가 입력하는 자리" 는 장식이 아니라 정보이므로 3:1 을 넘겨야 한다
(WCAG 1.4.11). `--field`(#828F8C, 3.36:1)를 새로 만들어 입력칸에만 쓴다.

### 8.2 터치 44px

**손가락으로 쓰는 기기에서만** 조작 요소의 높이를 44px 로 올린다
(`@media (pointer: coarse)`). 마우스에서는 촘촘한 쪽이 읽기 좋다.

`min-height` 는 인라인 요소에 적용되지 않으므로, 문단 속 링크는 그대로
두고 버튼처럼 배치된(inline-flex·block) 것만 커진다. 규칙 하나로 카테고리
칩·작은 버튼·하단 탭이 한꺼번에 해결됐다.

폭은 따로 손봐야 했다. 댓글의 `답글`·`수정`·`삭제` 는 글자 폭이 21px
밖에 안 됐다. 좌우에 8px 씩 여백을 주고 줄 전체를 `-mx-2` 로 당겨,
**보이는 위치는 그대로 두고 누를 수 있는 폭만** 넓혔다.

### 8.3 키보드 탐색

입력칸에 `outline-none` 이 걸려 있어 키보드로 이동하면 지금 어디에 있는지
알 수 없었다. 걷어내고 `globals.css` 한 곳에서 초점 테두리를 정한다 —
2px 브랜드색, 2px 바깥으로. `:focus-visible` 이라 마우스 클릭에는 나오지
않는다.

버튼의 `transition-colors` 에는 `outline-color` 가 들어 있어서, 흰 글자
버튼에 초점이 갈 때 테두리가 흰색에서 브랜드색으로 서서히 변했다 —
흰 바탕에서는 그동안 테두리가 보이지 않는다. 버튼만 전이 대상을
`background-color`·`color` 두 개로 좁혔다.

오류 메시지는 `role="alert"`, 성공 메시지는 `role="status"` 로 나눴다.
둘 다 `status` 면 읽기 프로그램이 오류를 즉시 읽지 않는다.

### 8.4 다시 잴 때

화면을 렌더한 뒤 실제 계산값으로 확인한다. 토큰 표만 보면 **겹쳐 쓴
배색**(브랜드면 위의 보조 텍스트)을 놓친다. 이번에 빠진 한 건이 정확히
그 경우였다.

---

## 9. 하지 않을 것

- 그라디언트 장식 — **버튼·카드 하나하나에** 넣지 않는다.
  화면 전체의 바탕 한 겹은 예외다 (§10.1)
- 카드마다 같은 그림자, 모든 요소에 같은 라운드
- 금색 광원·반짝임·파티클 — 금색은 **형태**로 쓰고 **빛**으로 쓰지 않는다
- 제목 속 한 단어만 색을 바꾸는 강조
- 섹션마다 페이드인 · 카드마다 hover 확대
- 버튼 텍스트 뒤의 `→`
- 실제 순서가 아닌 곳의 `01 / 02 / 03`
- 대문자 라벨
- **가운뎃점으로 이어 붙인 메타 줄** (§6 에서 걷어낸다)

## 10. 게임 톤을 게시판에 쓰는 법

첨부 참고자료는 삼국지 게임의 무장 제안 화면이다. 그 인상을 가져오되
**게시판으로 기능하게** 옮기는 규칙이다. 근거는 PRD §6.2.

### 10.1 바탕

평면 검정이 아니라 **위가 살짝 밝은 먹빛**이다. 화면 전체에 한 겹 깔린
방사형 그라디언트가 깊이를 만든다.

§9 의 "그라디언트 장식" 금지는 **버튼·카드 하나하나에** 넣지 말라는 뜻이다.
화면 바탕 한 겹은 그 금지 대상이 아니다 — 요소마다 들어가면 시끄럽지만,
바탕에 한 번 깔리는 것은 조명이지 장식이 아니다.

### 10.2 금색의 쓰임

**금색은 정보일 때만 쓴다.** 아무 데나 쓰면 금색이 아무 뜻도 없어진다.

| 쓰는 곳 | 뜻 |
|---|---|
| 카드·패널 테두리 | 여기가 하나의 덩어리다 |
| 헤더·하단 탭의 경계선 | 화면의 틀 |
| `공지` 배지 | 운영자가 올린 것 |
| 활성 카테고리의 링 | 지금 보고 있는 곳 |
| 보조 버튼 테두리 | 누를 수 있는 것 |

쓰지 않는 곳: 본문, 목록 제목, 일반 배지, 시각·닉네임 같은 메타 정보.

`--gold` 는 테두리용이고 `--gold-lit` 은 글자로 써도 되는 밝기다.
`--gold-dim` 은 늘 번쩍이지 않게 하려고 둔 어두운 금이다 — 대부분의
테두리는 이쪽을 쓰고, 윗변에만 `--gold` 를 준다.

### 10.3 패널은 테두리 한 겹까지

```
.panel = 면 색 + 1px 테두리 + 윗변만 진한 금
```

빛이 **위에서 온다**는 한 방향만 지키면 금속처럼 보인다. 사방을 다 밝히면
스티커가 된다. 안쪽 그림자·바깥 광원·이중 테두리를 겹치면 카드 하나가
액자가 되고, 목록처럼 카드가 여러 개 쌓이는 화면에서 **글보다 액자가 먼저
읽힌다** (PRD V-3).

### 10.4 가져오지 않은 것

| | 왜 |
|---|---|
| **화면에 고정된** 모서리 장식 | 게임은 화면이 고정이지만 게시판은 스크롤한다. 화면 네 귀에 붙는 장식은 계속 따라다닌다. **패널에 붙는 꺾쇠는 다르다** — 그건 같이 스크롤하므로 §10.5 에서 쓴다 |
| 인물 초상 | §3.3 — 인물을 그리면 화면이 무거워지고 특정 작품과 닮을 위험이 있다 |
| 반짝임·빛 번짐 | 글을 읽는 동안 움직이는 것이 있으면 안 된다 (PRD V-5) |
| 세로쓰기 이름 패널 | 한글 UI 에서 세로쓰기는 읽는 속도를 떨어뜨린다 |
| 나무결·양피지 텍스처 | 15px 글자 뒤에 텍스처가 깔리면 글자 획과 섞인다 |

공통점이 하나다 — **게임은 보는 화면이고 게시판은 읽는 화면이다.** 버린
것은 전부 **읽기와 경쟁하는** 것들이다.

### 10.5 장면과 표식 (v1.2.1)

먹빛과 금테만으로는 화면이 **금테 사각형의 반복**이 되었다. 게임 화면이
풍성한 이유는 색이 아니라 **장면**이 있기 때문이다. 그래서 장식을 넣되,
§2 의 원칙은 건드리지 않았다 — **읽는 길은 여전히 조용하다.**

#### 대담함은 한 곳에

| 자리 | 무엇이 있나 |
|---|---|
| **홈 히어로 · `/about` 히어로** | 달 · 상운 · 능선 두 겹 · 능선 위 깃발 · 그 앞의 언월도 로고 |
| 빈 목록 · 404 | 언월도 삽화 + 구름 띠 |
| 화면 제목 | 왼쪽 금색 표식 (`title-mark`) |
| 패널 모서리 | 금색 꺾쇠 네 개 |

목록 행·글 본문·댓글에는 **아무 장식도 없다.** 사용자가 글을 읽는 동안에는
브랜드가 말을 걸지 않는다.

#### 밤 풍경은 앞산이 더 어둡다

대기 원근이 반대로 걸리면(앞이 밝으면) 산이 아니라 **겹친 색종이**로 읽힌다.
뒤 산맥 `#24352F`, 앞 산맥 `#161F1C`.

#### 잘릴 것을 전제로 그리지 않는다

처음에는 산수를 히어로 아래쪽 62% 에만 깔고 `slice` 로 잘랐다. 그랬더니
**달이 잘려 사라졌다.** 잘릴 것을 전제로 그리면 무엇이 남을지 알 수 없다.

지금은 viewBox 를 **히어로의 실제 비율(800×230)에 맞춰** 그리고, 좁은
화면에서만 좌우가 잘리게 둔다. 모바일에서 달은 잘리지만 능선과 깃발이
남아 장면은 유지된다 — **무엇이 잘려도 되는지 정해 놓고 그린 것**이다.

#### 늘어나면 안 되는 것을 늘어나는 상자에 담지 않는다

모서리 꺾쇠를 처음에는 viewBox 100×100 짜리 SVG 하나에 네 개를 그리고
`preserveAspectRatio="none"` 으로 패널 크기에 맞춰 늘렸다. 그랬더니
**선 굵기까지 같이 늘어나** 가로로 뭉개진 두꺼운 ㄱ자가 되었다.

지금은 **고정 크기 조각 네 개**를 모서리에 각각 놓는다. 패널이 아무리
커지고 작아져도 꺾쇠는 20px 그대로다.

#### 이미지 파일을 쓰지 않는다

전부 SVG 컴포넌트다 — 틀 장식은
[Ornaments.tsx](src/components/brand/Ornaments.tsx), 그림은
[Scenery.tsx](src/components/brand/Scenery.tsx). 색 토큰을 따라가야 하고
(테마가 또 바뀔 수 있다), 어느 크기로 늘려도 뭉개지지 않아야 하고,
내려받을 것이 늘면 안 되기 때문이다.

### 10.6 인터페이스는 글자가 아니라 명판이다

참고자료(삼국지 게임)의 메뉴는 **판 위에 글자가 올라간 것**이지 글자에
링크가 걸린 것이 아니다. v1.2 는 그 차이를 놓치고 1px 금색 선만 그어서,
화면이 계속 **도면처럼** 보였다.

지금 메뉴와 버튼은 금속 판이다.

| 유틸리티 | 쓰는 곳 |
|---|---|
| `plaque` | 기본 판 — 카테고리 메뉴, 보조 버튼 |
| `plaque-on` | 눌린 판 — 지금 보고 있는 메뉴. 안쪽에서 청록이 배어 나온다 |
| `plaque-brand` | 청록 에나멜 — 주요 행동 (가입·글쓰기·등록) |

#### 그라디언트 테두리에 둥근 모서리를 함께 주는 법

테두리를 그라디언트로 칠하려면 `border-image` 를 쓰게 되는데, 그러면
`border-radius` 가 죽는다. 대신 **배경 두 장을 겹친다.**

```css
background:
  linear-gradient(…판 색…)   padding-box,
  linear-gradient(…금속…)    border-box;
border: 2px solid transparent;
border-radius: 10px;
```

테두리를 투명하게 두면 바깥 배경(`border-box`)이 테두리 자리에 비친다.

#### 빛은 위에서 온다

판 위쪽에 밝은 안쪽 선(`inset 0 1px 0`), 아래쪽에 그림자를 준다. **한
방향만 지키면** 금속으로 읽힌다. 사방을 다 밝히면 스티커가 된다.

#### 판을 쓰지 않는 자리

`취소` 처럼 물러나는 행동, 본문 속 링크, 목록 행. 모든 것이 판이면 판이
아무 뜻도 없어진다 — 금색을 정보일 때만 쓰는 것(§10.2)과 같은 이치다.

### 10.7 그림은 선이 아니라 칠이다

v1.2.1 이전의 장식은 전부 **1~2px 짜리 선**이었다. 선으로 그린 산은
도면이지 그림이 아니다. 지금은 반대로 간다 — 선을 거의 쓰지 않고
**그라디언트로 칠한 면을 여러 겹 쌓는다.** 동양 산수화가 먹의 농담으로
거리를 만드는 방식과 같다.

#### 겹은 넷이다

```
하늘  →  원산(안개에 묻힘)  →  중산  →  근산(실루엣)
밝고 흐리게 ────────────────────────→ 어둡고 또렷하게
```

사이사이에 **안개 띠**(`feGaussianBlur` 를 먹인 반투명 가로줄)를 넣어 겹을
끊는다. 안개가 없으면 산이 겹친 색종이로 보인다.

#### 직접 그린 SVG 는 폴백으로만 남았다

`Landscape` · `Seal` 은 **그림 파일이 없을 때만** 나온다 (§10.9). 코드로
그린 산은 도면이지 그림이 아니다 — 선 몇 개와 그라디언트로는 생성형 AI
로 뽑은 회화에 비교가 안 된다. 그 사실을 인정하는 데 세 번이 걸렸다.

지금 화면에 실제로 보이는 그림은 전부 `public/images/` 의 파일이다.

#### 그림 위의 글자는 어둠막으로 받친다

산수 위에 바로 글자를 얹으면 능선과 획이 섞인다. 글자가 놓이는 쪽에만
**가로 방향 어둠막**을 깔아 대비를 만든다. 막이 너무 두꺼우면 그림이
사라지므로, 글자 끝나는 지점부터 빠르게 투명해지게 둔다.

---

### 10.8 메뉴만 밝다 — 양피지

어두운 화면에서 **메뉴만 밝게** 띄우면 어디를 눌러야 하는지가 한눈에
보인다. 게임 UI 가 메뉴를 밝은 판으로 두는 이유가 이것이다.

`paper` · `paper-on` 유틸리티가 그 판이다. 판 위 글자는 먹색이라 대비가
13:1 을 넘어 작은 글씨도 또렷하다.

다만 **게시판 그림이 있으면 그림이 이긴다.** 양피지는 그림이 없을 때의
폴백이다 — 그림이 게시판마다 다른 인상을 남기는 쪽이 낫다.

### 10.9 그림은 파일, 코드는 폴백

큰 그림은 코드로 그릴 수 없다. 그래서 그 자리는 **생성형 AI 로 뽑은 그림
파일**이 채우고, 파일이 없으면 SVG 폴백이 나온다.

```
public/images/<이름>.<webp|avif|jpg|png>   →  src/lib/art.ts 가 찾는다
```

| 이름 | 쓰이는 곳 | 없으면 |
|---|---|---|
| `hero` | 홈·`/about` 머리 | `Landscape` SVG |
| `side-left` · `side-right` | PC 좌우 여백 | 아무것도 안 그림 |
| `header-arm` | 합성 로고의 무기 (§3.5) | 워드마크만 |
| `cat-<slug>` | 사이드바 메뉴 · 게시판 머리 | 양피지 판 (§10.8) |
| `notfound` | 404 | `Landscape` SVG |

**코드를 고치지 않고 그림만 갈아 끼울 수 있다.** 파일을 넣으면 그 순간
화면이 바뀐다.

#### 그림 위의 글자는 반드시 받친다

그림에 밝은 부분(등불·달)이 있으면 글자 획과 섞인다. 어둠막을 깔되
**넓은 자리는 그라디언트, 좁은 자리(칩)는 평평하게** 한다 — 좁은 곳에
그라디언트를 쓰면 글자 뒤 밝기가 들쭉날쭉해진다.

#### 시트에서 잘라 쓸 때 걸린 것

여러 장을 한 시트로 받아 잘랐는데, 타일마다 **시트의 패널 경계선**이
딸려 들어왔다. 색으로 판정해 지우려다 **은색 칼날을 통째로 날렸다** —
칼날도 무채색이라 배경 조건에 걸린 것이다.

> **가장자리 줄을 잘라내는 쪽이 안전하다.** 테두리는 가장자리에만 있고
> 그림 본체는 안쪽에 있다. 색으로 지우면 본체 중 같은 색을 가진 부분이
> 함께 사라진다.

---

## 11. 다 됐는지 보는 기준

- [x] 목록만 스크린샷으로 보면 **재미없어 보인다** (이게 맞는 상태다)
- [x] 로고를 20px 로 줄여도 언월도로 읽힌다 (v4 — 금·주홍을 넣고 해결)
- [x] 브랜드색이 상승·하락 색과 헷갈리지 않는다
- [x] 목록에서 숫자 자리가 맞는다 (제목 시작점·숫자 끝이 행마다 동일)
- [x] 제목이 두 줄로 넘쳐도 행 높이가 무너지지 않는다
- [x] 공지 3개가 연달아 있어도 답답하지 않다
- [x] 오류 화면이 웃기지 않고 무엇을 하면 되는지 알려준다
- [ ] 실기기(iOS · Android)에서 본문이 편하게 읽힌다 ← 남은 하나.
      **어두운 화면은 기기·밝기에 따라 차이가 크므로 v1.2 에서 더 중요해졌다**
- [x] 대비 4.5:1, 터치 44px, 키보드 탐색 가능 (§8 — 실측)
- [x] 화면 어디에도 `#3182f6` 이 남아 있지 않다
- [x] 같은 상황에 같은 문장을 쓴다 (빈 상태 · 오류 — §2.1 · §2.2)
- [x] 여백이 4px 배수 하나로 정리돼 있다 (§7)
- [x] 주요 버튼 글자가 **흰색이 아니다** — 밝은 청록 위 흰 글자는 2.4:1 (§8.1)
- [x] 로고 워드마크가 어두운 바탕에서 읽힌다 (§4.3 — 파일 색을 뒤집었다)
- [x] 금색이 **정보일 때만** 쓰인다 (§10.2)
- [x] 카드 테두리가 한 겹이다 — 액자가 되지 않았다 (§10.3)
